# Django 电商系统 - 订单状态与库存预警功能实现总结

## 一、功能概述

本实现为Django电商系统增加了两个核心功能：

1. **订单状态管理**：用户可实时查看订单状态（待处理、已发货、配送中、已完成），并可取消未发货订单
2. **库存预警系统**：后台自动识别库存低于阈值的商品，提醒管理员及时补货

## 二、关键代码实现

### 1. 模型层修改（core/models.py）

#### 新增订单状态选项
```python
ORDER_STATUS_CHOICES = (
    ('P', '待处理'),
    ('S', '已发货'),
    ('D', '配送中'),
    ('C', '已完成'),
)
```

#### Order模型新增字段
```python
status = models.CharField(
    max_length=1,
    choices=ORDER_STATUS_CHOICES,
    default='P'
)
cancelled = models.BooleanField(default=False)
```

#### Item模型新增字段
```python
stock = models.IntegerField(default=0)
stock_threshold = models.IntegerField(default=10)
```

### 2. 视图层实现（core/views.py）

#### 订单详情视图
```python
class OrderDetailView(LoginRequiredMixin, DetailView):
    model = Order
    template_name = 'order_detail.html'
    
    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['status_choices'] = ORDER_STATUS_CHOICES
        return context
```

#### 订单取消功能
```python
@login_required
def cancel_order(request, order_id):
    order = get_object_or_404(Order, id=order_id, user=request.user)
    
    # 仅允许取消待处理订单
    if order.status in ['P'] and not order.cancelled:
        order.cancelled = True
        order.save()
        messages.success(request, "订单已成功取消。")
    else:
        messages.warning(request, "该订单无法取消。")
    
    return redirect('core:order-detail', pk=order_id)
```

#### 用户订单列表
```python
@login_required
def user_orders(request):
    orders = Order.objects.filter(user=request.user, ordered=True).order_by('-ordered_date')
    return render(request, 'user_orders.html', {'orders': orders})
```

#### 库存预警视图
```python
from django.db.models import F
from django.contrib.admin.views.decorators import staff_member_required

@staff_member_required
def low_stock_alert(request):
    # 使用F表达式高效查询库存低于阈值的商品
    low_stock_items = Item.objects.filter(stock__lte=F('stock_threshold'))
    return render(request, 'low_stock_alert.html', {'items': low_stock_items})
```

### 3. URL配置（core/urls.py）

```python
path('order/<int:pk>/', OrderDetailView.as_view(), name='order-detail'),
path('order/cancel/<int:order_id>/', cancel_order, name='cancel-order'),
path('orders/', user_orders, name='user-orders'),
path('admin/low-stock/', low_stock_alert, name='low-stock-alert')
```

### 4. 模板文件

#### 订单详情模板（templates/order_detail.html）
- 显示订单状态、下单时间等信息
- 待处理订单显示"取消订单"按钮
- 已取消订单显示红色警告提示

#### 库存预警模板（templates/low_stock_alert.html）
- 表格形式展示低库存商品
- 显示商品名称、当前库存、库存阈值
- 提供直接编辑库存的后台链接

## 三、数据库迁移

迁移文件：`core/migrations/0005_auto_20260117_0047.py`

新增字段：
- `core_item.stock` (IntegerField, 默认0)
- `core_item.stock_threshold` (IntegerField, 默认10)
- `core_order.cancelled` (BooleanField, 默认False)
- `core_order.status` (CharField, 默认'P')

## 四、功能验证测试

### 测试脚本（test_order_status_final.py）

#### 订单状态管理测试
1. 创建测试用户和商品
2. 创建订单并验证初始状态为"待处理"
3. 测试状态流转：待处理→已发货→配送中→已完成
4. 测试待处理订单可取消
5. 验证已发货订单无法取消

#### 库存预警测试
1. 创建低库存商品（库存3，阈值10）
2. 创建正常库存商品（库存25，阈值10）
3. 创建临界库存商品（库存10，阈值10）
4. 使用F表达式查询低库存商品
5. 验证查询结果的准确性

### 测试执行结果

```
************************************************************
Django 电商系统 - 功能验证测试
************************************************************

============================================================
订单状态管理功能测试
============================================================

1. 创建测试用户: testuser_cnpwcnfr
2. 创建测试商品: Test Product (库存: 50, 阈值: 10)
3. 创建订单: #12，初始状态: 待处理
4. 添加商品到订单: Test Product x 2

5. 测试订单状态流转:
----------------------------------------
   ✓ 状态更新为: 已发货
   ✓ 状态更新为: 配送中
   ✓ 状态更新为: 已完成

6. 测试订单取消功能:
----------------------------------------
   创建待处理订单 #13
   ✓ 订单成功取消

   创建已发货订单 #14
   ✓ 已发货订单无法取消（符合业务逻辑）

============================================================
订单状态管理测试完成
============================================================

------------------------------------------------------------

============================================================
库存预警功能测试
============================================================

1. 创建低库存商品: Low Stock Item
   库存: 3 / 阈值: 10
   状态: ⚠ 低于阈值

2. 创建正常库存商品: Normal Stock Item
   库存: 25 / 阈值: 10
   状态: ✓ 正常

3. 创建临界库存商品: Threshold Item
   库存: 10 / 阈值: 10
   状态: ⚠ 低于阈值

4. 执行低库存查询（模拟后台预警功能）:
----------------------------------------
   找到 3 个低库存商品:
   1. A shirt - 库存: 0 / 阈值: 10
   2. Low Stock Item - 库存: 3 / 阈值: 10
   3. Threshold Item - 库存: 10 / 阈值: 10

5. 验证查询结果:
----------------------------------------
   ✓ Low Stock Item 被正确识别为低库存
   ✓ Normal Stock Item 未被错误标记（正确）
   ✓ Threshold Item 被正确识别（等于阈值）

============================================================
库存预警功能测试完成
============================================================

************************************************************
✅ 所有功能测试通过！

功能摘要:
  ✓ 订单状态管理 - 支持待处理、已发货、配送中、已完成状态流转
  ✓ 订单取消功能 - 仅待处理订单可取消
  ✓ 库存预警系统 - 自动识别低于阈值的商品
  ✓ F表达式查询 - 高效对比库存与阈值
************************************************************
```

## 五、技术亮点

1. **F表达式查询**：`Item.objects.filter(stock__lte=F('stock_threshold'))` 避免了Python层面的循环，直接在数据库层面进行字段对比，性能更优

2. **状态机设计**：订单状态通过choices字段实现，确保状态值的规范性和可维护性

3. **权限控制**：
   - `@login_required` 装饰器确保普通用户只能访问自己的订单
   - `@staff_member_required` 装饰器确保库存预警功能仅管理员可见

4. **用户体验**：
   - 订单详情页根据状态动态显示/隐藏取消按钮
   - 库存预警页面直接提供编辑链接，提升管理员操作效率

5. **数据完整性**：
   - 取消订单时检查订单状态，避免误操作
   - 数据库字段设置合理的默认值

## 六、后续优化建议

1. **订单状态日志**：记录订单状态变更历史，便于追踪和审计

2. **库存预警通知**：当商品库存低于阈值时，自动发送邮件或站内信提醒管理员

3. **库存自动补货建议**：基于销售数据预测补货时机和数量

4. **订单状态推送**：使用WebSocket实现订单状态实时推送，用户无需刷新页面即可查看最新状态

5. **批量操作**：支持管理员批量更新商品库存阈值

## 七、总结

本实现成功为Django电商系统增加了订单状态管理和库存预警功能，所有核心功能均通过测试验证。代码遵循Django最佳实践，使用了F表达式、装饰器、通用视图等特性，确保了代码的高效性和可维护性。